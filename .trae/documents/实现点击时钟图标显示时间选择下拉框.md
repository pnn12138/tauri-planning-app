1. **添加状态管理**：在TaskCreateModal组件中添加showTimeDropdown状态，用于控制时间选择下拉框的显示和隐藏

2. **修改UI结构**：
   - 调整时钟图标为可点击状态
   - 将时间选择器移到下拉框中
   - 添加下拉框容器和样式

3. **实现交互逻辑**：
   - 点击时钟图标切换showTimeDropdown状态
   - 点击下拉框外部关闭下拉框
   - 保持原有的时间选择功能

4. **更新样式**：
   - 设计下拉框样式，包括边框、阴影和定位
   - 确保下拉框与参考设计一致
   - 添加过渡动画效果

5. **优化用户体验**：
   - 确保下拉框在不同屏幕尺寸下都能正确显示
   - 添加焦点管理，确保键盘导航正常工作
   - 保持与现有设计系统的一致性

6. **测试功能**：
   - 验证点击时钟图标显示/隐藏下拉框
   - 验证时间选择功能正常
   - 验证下拉框样式符合参考设计

修改的文件：
- `src/features/task-create/TaskCreateModal.tsx`：添加状态管理和UI修改
- `src/features/task-create/taskCreateModal.css`：添加下拉框样式

这个实现将使时间选择更加简洁，只有在用户需要时才显示，符合参考设计的要求。