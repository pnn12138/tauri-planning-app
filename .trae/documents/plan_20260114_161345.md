## 修复计划

### 1. 移除未使用的 `scheduled_start_str` 变量
- **文件**: `src/services/planning_service.rs`
- **原因**: 这是我们之前修复frontmatter问题时产生的未使用变量
- **修复方案**: 直接移除该变量，在模板中直接使用`task.scheduled_start`

### 2. 删除未使用的导入
- **文件**: `src/repo/planning_repo.rs` - 移除`log::warn`导入
- **文件**: `src/services/planning_service.rs` - 移除`tracing::debug`和`tracing::warn`导入

### 3. 为未使用的函数添加 `#[allow(dead_code)]` 属性
- **UI状态相关**: `planning_get_ui_state`、`planning_set_ui_state`、`get_ui_state`、`set_ui_state`
- **其他**: `delete_task_md`、`merge_json`

## 实现步骤

1. 首先修复`scheduled_start_str`变量问题（优先级最高）
2. 然后修复未使用的导入
3. 最后为未使用的函数添加属性
4. 运行诊断检查确认修复有效

## 预期效果

- 消除所有与我们修复相关的警告
- 保持代码整洁度
- 确保现有功能不受影响
- 为未使用的函数添加适当的属性，便于未来扩展