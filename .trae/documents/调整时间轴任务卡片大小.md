1. **问题分析**：当前任务卡片高度设置了20%的最大限制，但用户反馈卡片仍然过大，需要进一步调整。

2. **解决方案**：优化任务卡片高度计算，降低最大高度限制，调整高度增长曲线。

3. **修改计划**：

   * 修改 `src/Home.tsx` 中已排期块的渲染逻辑：

     * 降低最大高度限制从20%到12%

     * 调整高度计算方式，让高度增长更缓慢

     * 保持最小高度40px，确保小任务可读性

   * 修改空闲块的渲染逻辑：

     * 降低最大高度限制从20%到12%

     * 保持高度与任务时长的关联

   * 确保样式优先级正确，没有被其他样式覆盖

4. **预期效果**：任务卡片高度更加合理，既能反映任务时长，又不会过大，整体视觉更加舒适。

