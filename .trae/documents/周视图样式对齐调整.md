# 周视图样式对齐调整计划

## 1. 分析当前周视图实现
当前周视图已基本实现，但样式需要调整以匹配参考设计：
- 时间轴宽度：当前580px，需调整
- 周头部样式：需优化星期和日期显示
- 时间轴列布局：需调整间隙和背景色
- 任务块样式：需优化高度、边框和阴影
- 空闲时间块：需调整显示方式

## 2. 调整内容

### 2.1 时间轴宽度调整
- 修改 `.view-mode-week .timeline-sidebar` 宽度，从580px调整为更宽的值
- 确保时间轴内容区域有足够空间显示7天

### 2.2 周头部样式优化
- 调整 `.timeline-week-day-header` 样式
- 优化 `.timeline-week-day-name` 和 `.timeline-week-day-date` 字体、颜色
- 增强今日日期的高亮效果

### 2.3 时间轴列布局调整
- 调整 `.timeline-week-columns` 间隙
- 优化 `.timeline-week-column` 背景色和边框
- 确保列宽均匀分布

### 2.4 任务块样式优化
- 调整 `.timeline-week-column .timeline-block` 左右边距
- 优化任务块高度和边框样式
- 增强任务块的视觉层次感

### 2.5 空闲时间块调整
- 简化空闲时间块显示，移除不必要的文字
- 调整空闲时间块的背景色和边框样式

### 2.6 时间刻度样式调整
- 优化 `.timeline-week-time-scale` 宽度和位置
- 调整 `.timeline-week-time-item` 字体和颜色
- 确保时间刻度与任务块对齐

## 3. 实现步骤
1. 修改App.css中周视图相关样式
2. 调整时间轴宽度和列布局
3. 优化头部和时间刻度样式
4. 调整任务块和空闲时间块样式
5. 测试不同屏幕尺寸下的显示效果

## 4. 预期效果
- 周视图与参考样式对齐
- 7天时间轴清晰可见
- 任务块和空闲时间块布局合理
- 时间刻度与内容对齐
- 整体视觉效果美观协调