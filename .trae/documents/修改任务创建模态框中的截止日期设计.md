1. **修改截止日期输入方式**：将 TaskCreateModal.tsx 中的日期+时间分开选择的设计，改为使用 `datetime-local` 类型的输入框，实现日期和时间的统一选择
2. **删除小时钟图标按钮**：移除 `task-create-time-selector` 元素及其内部的小时钟图标按钮和时间下拉菜单
3. **更新状态管理**：修改 `updateDraft` 函数，将 datetime-local 输入值正确解析为日期和时间字段
4. **调整样式**：移除相关的 CSS 类，确保新的输入框样式与其他表单元素保持一致
5. **验证功能**：确保修改后的截止日期输入能够正确创建任务，并且时间精度达到时分级别

