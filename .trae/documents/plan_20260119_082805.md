1. **修改周视图切换按钮，只保留图标**

   * 移除按钮中的"这周"文字，只保留日历图标

   * 确保按钮样式适配只有图标的情况

2. **实现视图模式对时间轴渲染的影响**

   * 修改timelineModel的构建逻辑，根据viewMode决定是构建日视图还是周视图

   * 创建或扩展buildTimelineModel函数以支持周视图

   * 调整时间轴渲染部分，根据viewMode显示不同的内容

3. **更新时间轴布局**

   * 当日视图时，保持现有布局不变

   * 当周视图时，显示一周的日期列

   * 确保时间刻度和任务块在周视图中正确排列

4. **测试和优化**

   * 确保按钮点击能切换视图模式

   * 验证不同视图模式下的显示效果

   * 优化样式，确保周视图美观易用

