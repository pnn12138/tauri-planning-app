**问题分析**：
- 目前只有待做（todo）和已完成（done）列的任务卡片可以互相拖拽
- 待办（backlog）和进行中（doing）列无法参与拖拽
- 原因：在`handleDragEnd`函数中，代码明确禁止了从doing列拖拽或拖拽到doing列
- 但对于backlog列，代码中没有明确的限制，可能是由于逻辑错误导致无法正确处理backlog列的拖拽

**修复方案**：
1. 修改`handleDragEnd`函数，允许从backlog列拖拽任务到其他列
2. 确保当拖拽到backlog列时，代码能够正确获取目标列信息
3. 保持doing列的拖拽限制不变

**修复步骤**：
1. 检查`handleDragEnd`函数中处理backlog列的逻辑
2. 确保当over是backlog列中的任务时，代码能够正确获取targetColumnId
3. 确保当over是backlog列容器时，代码能够正确处理拖拽
4. 测试修复后的拖拽功能，确保四列任务卡片可以互相拖拽（除了doing列）

**预期效果**：
- backlog、todo和done列的任务卡片可以互相拖拽
- doing列仍然禁止拖拽，保持原有的Start/Stop按钮管理方式
- 拖拽体验流畅，视觉效果正常