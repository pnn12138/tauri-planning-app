## 问题分析

1. **"今日"右侧按钮**实际上包含两个部分：
   - 左侧的两个**导航箭头按钮**（无点击事件）
   - 右侧的**时间线折叠按钮**（有点击事件但缺少视觉反馈）

2. **具体问题**：
   - 导航箭头按钮（左右箭头）没有实现任何点击事件处理函数，导致点击无反应
   - 时间线折叠按钮虽然有点击事件，但缺少对应的CSS样式，点击后没有明显的视觉反馈

3. **代码位置**：
   - 导航箭头按钮：`src/Home.tsx:1544-1553`
   - 时间线折叠按钮：`src/Home.tsx:1557-1572`
   - 相关状态管理：`updateUIState` 函数（来自 `planning.store.ts`）

## 解决方案

### 1. 为导航箭头按钮添加点击事件

为左侧的两个导航箭头按钮添加点击事件处理函数，实现日期导航功能：

```javascript
// 在 Home.tsx 中找到导航按钮并添加 onClick 事件
<button className="timeline-nav-btn" onClick={handlePrevDay}>
  {/* 左箭头图标 */}
</button>
<button className="timeline-nav-btn" onClick={handleNextDay}>
  {/* 右箭头图标 */}
</button>
```

### 2. 实现日期导航逻辑

在 Home.tsx 中添加日期导航函数：

```javascript
const handlePrevDay = () => {
  // 实现向前导航一天的逻辑
};

const handleNextDay = () => {
  // 实现向后导航一天的逻辑
};
```

### 3. 为时间线折叠按钮添加CSS样式

在 `src/App.css` 中添加时间线折叠按钮的样式：

```css
.timeline-collapse-btn {
  /* 基础样式 */
  padding: 4px;
  color: #94a3b8;
  background: transparent;
  border: 1px solid transparent;
  border-radius: 4px;
  cursor: pointer;
  transition: all 0.2s ease;
}

.timeline-collapse-btn:hover {
  /* 悬停样式 */
  color: #1e293b;
  background-color: #f1f5f9;
}

.timeline-collapse-btn.collapsed {
  /* 折叠状态样式 */
  /* 添加适当的视觉反馈 */
}
```

### 4. 确保时间线折叠功能正常工作

检查 `timelineCollapsed` 状态是否正确影响时间线的显示/隐藏：

```javascript
// 确保 sidebar 的 collapsed 类有对应的样式
.timeline-sidebar.collapsed {
  /* 折叠状态的样式，如宽度变化 */
}
```

## 预期效果

1. 点击左侧导航箭头按钮时，时间线会向前/向后导航一天
2. 点击右侧折叠按钮时，时间线侧边栏会折叠/展开，并有明显的视觉反馈
3. 所有按钮点击时都有响应，提升用户体验

## 实施步骤

1. 查看当前代码中是否已有日期导航相关的状态和函数
2. 为导航箭头按钮添加点击事件处理
3. 实现日期导航逻辑
4. 为时间线折叠按钮添加CSS样式
5. 测试所有按钮的功能和视觉反馈

## 相关文件

- `src/Home.tsx`：修改按钮点击事件和添加导航逻辑
- `src/App.css`：添加折叠按钮样式
- `src/features/planning/planning.store.ts`：可能需要添加日期导航相关的状态和函数