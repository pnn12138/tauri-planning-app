1. **分析问题**：

   * 周视图无法显示除今天外的任务：可能是因为 `loadTodayData` 函数只加载当天数据，而不是整个星期的数据

   * 日期与列不对齐：可能是因为 CSS 样式中日期头部和时间轴列的宽度计算不一致，或者没有考虑左侧时间刻度的宽度

2. **检查相关代码**：

   * 检查 `loadTodayData` 函数的实现，看看它是如何加载数据的

   * 检查周视图的 CSS 样式，特别是日期头部和时间轴列的宽度设置

3. **修复方案**：

   * 对于任务显示问题：修改数据加载逻辑，确保在周视图模式下加载整个星期的数据

   * 对于对齐问题：调整 CSS 样式，确保日期头部和时间轴列宽度一致，并考虑左侧时间刻度的宽度

4. **预期效果**：

   * 周视图能够显示整个星期的任务

   * 日期头部与下方的时间轴列对齐

   * 界面样式与参考图一致

5. **修改文件**：

   * `src/features/planning/planning.store.ts`：检查并修改 `loadTodayData` 函数

   * `src/App.css`：调整周视图的 CSS 样式

   * `src/Home.tsx`：可能需要修改周视图的渲染逻辑

6. **验证方法**：

   * 切换到周视图，检查是否显示整个星期的任务

   * 检查日期头部与时间轴列是否对齐

   * 确保界面样式与参考图一致

