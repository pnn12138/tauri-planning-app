1. **分析问题**：

   * 当前右侧看板区域（kanban-section）在周视图中占比 40%，用户希望它更窄一些

   * 布局使用了 flexbox 来控制左侧时间轴和右侧看板的比例

2. **检查相关代码**：

   * 查看 `src/App.css` 中关于 `timeline-sidebar` 和 `kanban-section` 的 CSS 样式

   * 特别是周视图模式下的比例设置

3. **修复方案**：

   * 调整 `timeline-sidebar` 和 `kanban-section` 的 flex 比例

   * 减少 `kanban-section` 的 `flex-basis` 和 `flex-grow` 值

   * 增加 `timeline-sidebar` 的相应值

4. **预期效果**：

   * 右侧看板区域变窄

   * 左侧时间轴区域变宽

   * 整体布局更加符合用户需求

5. **修改文件**：

   * `src/App.css`：调整周视图的 flex 比例设置

6. **验证方法**：

   * 切换到周视图，检查右侧看板区域是否变窄

   * 确保整体布局仍然美观和可用

