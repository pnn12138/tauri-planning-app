## 修改任务优先级为P0/P1/P2/P3

### 1. 后端修改

**文件：`src-tauri/src/domain/planning.rs`**
- 更新 `TaskPriority` 枚举，添加 `Urgent` 类型
- 更新 `From<&str>` 实现，支持从字符串转换为新的优先级枚举
- 更新 `Display` 实现，将枚举值转换为对应的P0/P1/P2/P3字符串

### 2. 前端修改

**文件：`src/shared/types/planning.ts`**
- 更新 `TaskPriority` 类型，从 `'high' | 'medium' | 'low'` 改为 `'p0' | 'p1' | 'p2' | 'p3'`

**文件：`src/features/task-create/taskCreateModal.types.ts`**
- 更新 `TaskCreateDraftStep1` 中的 `priority` 字段类型，从 `'urgent' | 'high' | 'medium' | 'low'` 改为 `'p0' | 'p1' | 'p2' | 'p3'`
- 更新 `TaskDraft` 中的 `priority` 字段类型，从 `'high' | 'medium' | 'low'` 改为 `'p0' | 'p1' | 'p2' | 'p3'`
- 更新 `toCreateTaskInputStep1` 函数，确保优先级映射正确

**文件：`src/features/task-create/TaskCreateModal.tsx`**
- 更新初始优先级从 `'low'` 改为 `'p3'`
- 更新UI中的优先级选项显示，从中文名称改为P0/P1/P2/P3

**文件：`src/Home.tsx`**
- 更新任务编辑菜单中的优先级选项，从中文名称改为P0/P1/P2/P3
- 更新 `handleUpdatePriority` 函数中的优先级值

### 3. 优先级映射关系

| 旧优先级 | 新优先级 |
|---------|---------|
| urgent  | P0      |
| high    | P1      |
| medium  | P2      |
| low     | P3      |

### 4. 关键修改点

- 确保前后端优先级值一致
- 确保新建任务面板和任务编辑菜单中的优先级选项显示正确
- 确保数据库存储和查询时优先级值转换正确
- 确保默认优先级设置正确（P3）

### 5. 测试要点

- 新建任务时，优先级默认为P3
- 编辑任务时，可以选择P0/P1/P2/P3
- 任务保存后，优先级显示正确
- 任务状态变更时，优先级保持不变
- 数据库中优先级存储正确