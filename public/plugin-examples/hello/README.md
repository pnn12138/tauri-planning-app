# Hello Plugin (example)

Copy this folder to your vault:

`<VAULT>/.yourapp/plugins/hello/`

Then enable it from the Plugins panel, open the Command Palette, and run:

`Hello Plugin: Write .yourapp/hello.txt`

